spring:
  datasource:
    url: jdbc:mysql://localhost:3306/prjt01?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: prjt
    password: prjt1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
mybatis:
  mapper-locations: classpath:/mybatis/**/*.xml
  type-aliases-package: com.noonoo.prjtbackend
  configuration:
    map-underscore-to-camel-case: true
server:
  port: 8080

decorator:
  datasource:
    p6spy:
      enable-logging: true

logging:
  level:
    p6spy: info

